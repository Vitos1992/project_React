1. Відображається список фільмів, до 10 фільміф на кожній сторінці
2. Кожний фільм має в собі поля:
    - назва фільму,
    - режесер,
    - рік випуску
3. По натиснуню на фільм відкриваємо окрему сторінку фільму де будуть відображенні більше данних
4. Зробити все без антифікації (режим гістя)

+
Д/З
+

5. Проглянути та ознайомитися на практиці в терміналі 20 команд
6. Дізнатися які типи кондних терміналів існує (zsh, powershell, bash), які основні командні інтерфейси 
7. install gimini cli
8. авторизація vs автентифікація, в чому різниця, для чого, коли, як використовується
9. розібратися чому 

    const movies = await Promise.all(
        pageItems.map(async (movie) => ({
            id: movie.id,
            title: movie.title || movie.original_title,
            releaseYear: movie.release_date ? movie.release_date.split('-')[0] : 'Н/Д',
            director: await fetchMovieDirector(movie.id),
            posterPath: movie.poster_path
        }))
    );

    чому це погано.

10. Розібратиця в методі JS map для масивів та розуміти різницю між "Map" як структорую данних (дивитися Learn.js, спілкуватися з AI)
11. Пройти урок англійскої.
12. Розібратися із методами масивів filter, find, shift, unshift, pop, push, slice, splice,  map,
13. Методи що мутують масив та яки не мутують різниця між ними та приклади.


<!-- ==================home work=================== -->

🏠 Домашнє завдання — Початковий рівень
Напиши код для кожного завдання і покажи мені — розберемо разом.

1. Є масив [2, 4, 6, 8, 10]. Додай до кожного числа 5. Очікуваний результат: [7, 9, 11, 13, 15]
2. Є масив [15, 3, 9, 21, 6, 18]. Залиш тільки числа, які більше 10. Очікуваний результат: [15, 21, 18]
3. Є масив ['cat', 'dog', 'elephant', 'ant']. Знайди перше слово, яке має більше 3 літер. Підказка: str.length
4. Є масив [10, 20, 30, 40]. Порахуй суму всіх елементів через reduce. Очікуваний результат: 100
5. Є масив [2, 5, 8, 1, 4]. Перевір — чи всі елементи більше 0? Очікуваний результат: true
6. Є масив ['JS', 'Python', 'Go', 'Rust']. Виведи кожен елемент у консоль через forEach у форматі: Мова: JS, Мова: Python і т.д.

const result1 = arr1.map(el => el + 5); 
console.log(result1);

let result2 = arr2.filter(el => el > 10); 
console.log(result2);

let result3 = arr3.find(el => el.length > 3); 
console.log(result3);

let result4 = arr4.reduce((i, el) => i + el, 0); 
console.log(result4);

let result5 = arr5.every(el => el > 0);
console.log(result5);

